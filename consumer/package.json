{
  "name": "kafka-otel-consumer",
  "version": "1.0.0",
  "description": "Test environment for otel related kafka things - linking spans etc",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild main.ts --bundle --platform=node --target=node20 --outfile=Build/consumer.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.5",
    "vitest": "^0.34.6"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.7.0",
    "@opentelemetry/sdk-node": "^0.45.1",
    "@opentelemetry/sdk-trace-base": "^1.18.1",
    "kafkajs": "^2.2.4"
  }
}
